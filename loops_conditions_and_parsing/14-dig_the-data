#!/usr/bin/env bash
# This script groups visitors by IP and HTTP status code from an Apache log

# Use awk to extract the IP and HTTP status code, count occurrences, and sort by count
awk '{print $1, $9}' access.log | awk '{count[$1 " " $2]++} END {for (entry in count) print count[entry], entry}' | sort -nr
